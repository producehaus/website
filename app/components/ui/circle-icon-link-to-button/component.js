import Ember from 'ember';

const CircleIconLinkToButton = Ember.Component.extend({});

CircleIconLinkToButton.reopenClass({
  positionalParams: ['icon', 'link']
});

export default CircleIconLinkToButton;
